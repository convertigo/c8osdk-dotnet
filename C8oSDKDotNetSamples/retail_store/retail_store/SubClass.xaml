<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="retail_store.SubClass">

  <Grid BackgroundColor="#FFFFFF">
    
    
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <StackLayout Grid.Row="0" Grid.Column="0">
      <Image Source=""/>
    </StackLayout>

    <StackLayout Grid.Row="1"  Grid.Column="0" BackgroundColor="#531549" >
      <Label x:Name="labelC" Text="{Binding Category}"  />
    </StackLayout>

    <StackLayout  Grid.Row="2" Grid.Column="0">
      <ListView x:Name="listView" ItemTapped="OnItemTapped">
        <ListView.ItemTemplate>
          <DataTemplate>
            <ViewCell>
              <StackLayout Orientation="Horizontal" Padding="5, 0">
                <Label Text="{Binding subClass}" HorizontalOptions="StartAndExpand" VerticalOptions="Center" TextColor="#000000" FontSize="12"/>
              </StackLayout>
            </ViewCell>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </StackLayout>
    
  </Grid>
</ContentPage>