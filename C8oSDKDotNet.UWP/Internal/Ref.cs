using System;

namespace C8oSDKDotNet.UWP
{
    class Ref
    {
        Type stateless = typeof(Stateless.StateMachine<object, object>);
        Type sqlite = typeof(SQLitePCL.sqlite3);
        Type raw = typeof(SQLitePCL.raw);
    }
}
